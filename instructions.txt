#join all lines with consecutive text
%s/\n\(\w\)/ \1/g

#delete all lines that contain only numerics or whitespace
g/^[0-9\w]*$/d


#delete all non breaking spaces
%s/&nbsp;//g

#delete all double spaces (not real spaces) that were hyphenations
%s/­ //g

